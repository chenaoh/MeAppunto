<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.quindio.sena.meappunto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".actividades.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".actividades.SeleccionaUsuarioActivity" />
        <activity android:name=".actividades.RegistroAprendizActivity" />
        <activity android:name=".actividades.RegistroEmpresarioActivity" />
        <activity android:name=".actividades.LoginActivity" />
        <activity android:name=".actividades.SesionActivity" />
        <activity
            android:name=".actividades.SesionEmpresarioActivity"
            android:label="@string/title_activity_sesion_empresario"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>